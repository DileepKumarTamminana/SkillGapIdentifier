🔥 Modify Lambda to JOIN Employees Table

Add this inside your pending-requests Lambda (Python):

emp = employees_table.get_item(Key={"employeeId": item["employeeId"]}).get("Item", {})
item["employeeName"] = emp.get("name", "Unknown")


Full updated block:

response_items = []
for item in items:
    emp = employees_table.get_item(Key={"employeeId": item["employeeId"]}).get("Item", {})
    
    response_items.append({
        "requestId": item["requestId"],
        "employeeId": item["employeeId"],
        "employeeName": emp.get("name", "Unknown"),
        "skillName": item.get("skillName"),
        "certificationName": item.get("certificationName"),
        "expiryDate": item.get("expiryDate"),
        "requestedAt": item.get("requestedAt")
    })
